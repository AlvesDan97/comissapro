services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: comiss
      POSTGRES_PASSWORD: comiss
      POSTGRES_DB: comiss
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
